{% extends 'base.html.twig' %}

{% block title %}Update Publisher - {{publisher}}{% endblock %}

{% block body %}
    <div class="row my-3">
        <div class="col-12 mb-3">
            <h1>Update Publisher - {{publisher}}</h1>
            {{ include('publisher/_form.html.twig', {'button_label': 'update'}) }}
        </div>
    </div>
{% endblock %}
