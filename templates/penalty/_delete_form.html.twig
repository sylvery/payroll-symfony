<form method="post" action="{{ path('penalty_delete', {'id': penalty.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ penalty.id) }}">
    <button class="btn">Delete</button>
</form>
