<form method="post" action="{{ path('reading_level_delete', {'id': reading_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reading_level.id) }}">
    <button class="btn">Delete</button>
</form>
